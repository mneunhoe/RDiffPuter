% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utility-functions.R
\name{resolve_device}
\alias{resolve_device}
\title{Resolve a torch device string with fallback}
\usage{
resolve_device(device = "cpu")
}
\arguments{
\item{device}{One of \code{"cpu"}, \code{"cuda"}, or \code{"mps"}.}
}
\value{
The resolved device string. Always one of \code{"cpu"}, \code{"cuda"}, \code{"mps"}.
}
\description{
Validate a user-supplied device string and silently fall back to
\code{"cpu"} (with a warning) when CUDA or MPS is requested but unavailable. This
mirrors the pattern used by the \code{RGAN} package so that calls succeed on any
machine.
}
\examples{
\dontrun{
resolve_device("cuda")  # -> "cuda" if available, else "cpu" with warning
}
}
