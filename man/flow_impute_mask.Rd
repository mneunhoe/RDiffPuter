% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flow-utils.R
\name{flow_impute_mask}
\alias{flow_impute_mask}
\title{Conditional sampling for missing-value imputation (flow matching)}
\usage{
flow_impute_mask(
  net,
  x,
  mask,
  num_steps = 10L,
  num_resamplings = 5L,
  resample_strategy = "repaint",
  device = "cpu"
)
}
\arguments{
\item{net}{A \code{\link[=flow_model]{flow_model()}} instance.}

\item{x}{A torch tensor of observed values; missing entries should already
be zeroed.}

\item{mask}{A torch tensor of the same shape as \code{x}. \code{1} marks \strong{missing}
entries, \code{0} marks observed.}

\item{num_steps}{Number of outer Euler steps.}

\item{num_resamplings}{Inner re-flow iterations per outer step. Honoured
only when \code{resample_strategy = "repaint"}.}

\item{resample_strategy}{\code{"repaint"} (default) or \code{"single"}.}

\item{device}{Torch device.}
}
\value{
A torch tensor of the same shape as \code{x} containing reconstructed
values for the missing entries.
}
\description{
Flow-matching analogue of \code{\link[=impute_mask]{impute_mask()}}. At each Euler step
the latent is split: observed entries advance along the deterministic
linear interpolant \code{(1 - t) * x_0 + t * x_obs}, while missing entries
advance via the model's velocity. The two halves are re-glued via the
missingness mask.
}
\details{
Two \code{resample_strategy} options:
\itemize{
\item \code{"repaint"} (default): runs an inner loop of length \code{num_resamplings} per
outer Euler step that re-flows the latent backward and re-steps forward,
the flow analogue of RePaint's re-noising loop. Slower, but provides the
variance-reduction benefit familiar from the EDM imputer.
\item \code{"single"}: a single Euler step per outer step; \code{num_resamplings} is
ignored. Cheapest and fastest; practical for users who run
\code{num_trials > 1} and rely on the trial average for variance reduction.
}

This is a heuristic adaptation of the RePaint construction; it is not the
exactness guarantee of DiffPuter's Theorem 1, which was derived for the
EDM diffusion case. Empirically the heuristic matches the EDM imputer's
quality at ~1/5 the sampling steps; for theoretical fidelity use
\code{method = "edm"}.
}
