% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-persistence.R
\name{save_diffputer}
\alias{save_diffputer}
\title{Save a trained DiffPuter model and its data transformer}
\usage{
save_diffputer(object, path, transformer = NULL)
}
\arguments{
\item{object}{A \code{trained_RDiffPuter} (from \code{\link[=diffputer_trainer]{diffputer_trainer()}}) or an
\code{RDiffPuter_em} (from \code{\link[=diffputer_em]{diffputer_em()}}).}

\item{path}{Path stem (e.g. \code{"models/run1"}); the function appends
\verb{_state.pt} and \verb{_meta.rds}.}

\item{transformer}{Optional \code{data_transformer} to save alongside. Required
if \code{object} is a \code{trained_RDiffPuter}; ignored for \code{RDiffPuter_em} (which
already carries its transformer).}
}
\value{
The character vector \code{c(state_path, meta_path)} invisibly.
}
\description{
Persists the components needed to resume imputation in a later
R session. The diffusion model \code{state_dict} is saved via
\code{torch::torch_save()} (\code{.pt} file), the fitted transformer and bookkeeping
metadata via \code{saveRDS()} (\code{.rds} file). Files share a common stem.
}
