% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/em-imputer.R
\name{diffputer_em}
\alias{diffputer_em}
\title{Diffusion-based EM imputation for arbitrary tabular data}
\usage{
diffputer_em(
  data,
  method = "edm",
  max_iter = 10L,
  epochs = 10000L,
  patience = 500L,
  hidden_dim = 1024L,
  batch_size = 4096L,
  base_lr = 1e-04,
  num_steps = NULL,
  num_trials = NULL,
  num_resamplings = NULL,
  resample_strategy = "repaint",
  discrete_columns = NULL,
  encoding = "binary",
  device = "cpu",
  seed = NULL,
  checkpoint_path = NULL,
  validation_data = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame whose missing entries are encoded as \code{NA}.
Continuous columns must be numeric; categorical columns are auto-detected
from \code{factor}, \code{character}, or \code{logical} types.}

\item{method}{Generative model. \code{"edm"} (default, DiffPuter-faithful) or
\code{"flow"} (flow matching with linear interpolant). Flow models use fewer
sampling steps with comparable quality on most tabular data.}

\item{max_iter}{Number of EM iterations.}

\item{epochs}{Maximum training epochs per M-step.}

\item{patience}{Early-stopping patience (epochs without loss improvement).}

\item{hidden_dim}{Hidden width of the MLP denoiser.}

\item{batch_size}{Mini-batch size during training.}

\item{base_lr}{Adam learning rate.}

\item{num_steps}{Outer denoising / Euler steps per E-step trial. Defaults
to 50 for EDM, 10 for flow when left \code{NULL}.}

\item{num_trials}{Independent reconstruction trials averaged in each E-step.
Defaults to 20 (EDM) / 10 (flow) when left \code{NULL}.}

\item{num_resamplings}{Inner RePaint iterations per outer step. Defaults to
20 (EDM) / 5 (flow) when left \code{NULL}.}

\item{resample_strategy}{For \code{method = "flow"} only: \code{"repaint"} (default,
inner re-flow loop) or \code{"single"} (one Euler step per outer step).}

\item{discrete_columns}{Optional character vector of column names to treat as
discrete; otherwise auto-detected.}

\item{encoding}{Categorical encoding, \code{"binary"} (default, DiffPuter-faithful)
or \code{"onehot"}.}

\item{device}{One of \code{"cpu"}, \code{"cuda"}, \code{"mps"}. Falls back to \code{"cpu"} with
a warning if unavailable.}

\item{seed}{Optional integer RNG seed.}

\item{checkpoint_path}{Optional path; if set, the best per-iteration model
state is saved here.}

\item{validation_data}{Optional held-out data frame in the same schema.
When supplied, MAE / RMSE on its missing entries are tracked per
iteration. (Held-out NAs in \code{validation_data} mark cells whose ground
truth in \code{data} is observed.)}

\item{verbose}{If \code{TRUE}, prints a per-iteration summary.}
}
\value{
A list with class \code{RDiffPuter_em} containing:
\describe{
\item{imputed}{Final imputed data frame.}
\item{model}{Last-iteration \code{trained_RDiffPuter}.}
\item{transformer}{The fitted \code{data_transformer}.}
\item{history}{Per-iteration list with \code{loss}, \code{mae}, \code{rmse}.}
}
}
\description{
High-level entry point. Given a data frame with \code{NA} entries,
fits a diffusion model in an Expectation-Maximisation loop and returns the
imputed data frame in the original schema. Continuous and categorical
columns are handled automatically. Runs on CPU, CUDA, or Apple Silicon
(MPS) via the \code{torch} package.
}
\details{
Each iteration alternates an \strong{M-step} (training a diffusion denoiser on
the current best estimate of the data, with missing entries filled by the
previous iteration's reconstruction) with an \strong{E-step} (drawing
\code{num_trials} RePaint-style samples and averaging them). Iteration 0 fills
missing entries with \code{0}, matching the reference implementation. Use the
lower-level \code{\link[=diffputer_trainer]{diffputer_trainer()}} / \code{\link[=impute]{impute()}} pair for finer control.
}
\examples{
\dontrun{
  set.seed(1)
  data <- data.frame(a = rnorm(200), b = runif(200, 1, 5),
                     c = factor(sample(letters[1:3], 200, replace = TRUE)))
  data$a[sample.int(200, 30)] <- NA
  data$c[sample.int(200, 20)] <- NA
  result <- diffputer_em(data, max_iter = 2, epochs = 100,
                         hidden_dim = 64, num_trials = 4,
                         num_steps = 10, num_resamplings = 5,
                         batch_size = 64, patience = 20)
  head(result$imputed)
}
}
\references{
Zhang, H. et al. "DiffPuter: Empowering Diffusion Models for
Missing Data Imputation." ICLR 2025.
}
