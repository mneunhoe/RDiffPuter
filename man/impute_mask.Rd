% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diffusion-utils.R
\name{impute_mask}
\alias{impute_mask}
\title{Conditional sampling for missing-value imputation}
\usage{
impute_mask(
  net,
  x,
  mask,
  num_steps = 50L,
  num_resamplings = 20L,
  device = "cpu"
)
}
\arguments{
\item{net}{A preconditioned denoiser.}

\item{x}{A torch tensor of observed values; missing entries should already
be set to zero.}

\item{mask}{A torch tensor of the same shape as \code{x}. \code{1} marks \strong{missing}
entries, \code{0} marks observed (matching the reference implementation).}

\item{num_steps}{Number of outer denoising steps.}

\item{num_resamplings}{Number of inner re-noising iterations per outer step.}

\item{device}{Torch device.}
}
\value{
A torch tensor of the same shape as \code{x} containing reconstructed
values for the missing entries (observed entries are not preserved here;
the caller is expected to overlay the original observed values).
}
\description{
RePaint-style sampler. At each outer denoising step the latent
is split: known features are re-noised from the observed values while
unknown features advance through one Heun EDM step. The two halves are
re-glued via the missingness mask. An inner resampling loop of length
\code{num_resamplings} (N=20 in the reference paper) helps match the marginal
to the data distribution. Direct port of \code{impute_mask()} from
\code{DiffPuter/diffusion_utils.py}.
}
