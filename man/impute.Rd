% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute-data.R
\name{impute}
\alias{impute}
\alias{impute.trained_RDiffPuter}
\title{Impute missing values with a trained diffusion model}
\usage{
impute(object, data, transformer, ...)

\method{impute}{trained_RDiffPuter}(
  object,
  data,
  transformer,
  num_trials = NULL,
  num_steps = NULL,
  num_resamplings = NULL,
  resample_strategy = "repaint",
  device = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{trained_RDiffPuter} object as returned by
\code{\link[=diffputer_trainer]{diffputer_trainer()}} or \code{\link[=diffputer_em]{diffputer_em()}}.}

\item{data}{A data frame whose schema matches the one used to fit
\code{transformer}. May contain \code{NA}s in any subset of columns.}

\item{transformer}{A fitted \link{data_transformer} (R6 instance).}

\item{...}{Currently unused.}

\item{num_trials}{Number of independent reconstruction trials whose mean is
used as the imputation. Higher values reduce sampling variance.}

\item{num_steps}{Number of outer denoising steps per trial.}

\item{num_resamplings}{Inner RePaint iterations per outer step. For
flow-trained models with \code{resample_strategy = "single"} this is ignored.}

\item{resample_strategy}{For flow-trained models: \code{"repaint"} (default,
inner re-flow loop) or \code{"single"} (one Euler step per outer step). Has
no effect when the model was trained with \code{method = "edm"}.}

\item{device}{Device to run sampling on. Defaults to whatever the model was
trained on.}
}
\value{
A data frame with the same column names and types as \code{data}, with
\code{NA} entries replaced by sampled imputations and observed entries left
unchanged.
}
\description{
S3 method that runs the E-step of DiffPuter on a data frame
containing \code{NA}s. Encodes the data via \code{transformer}, performs
\code{num_trials} independent RePaint-style sampling passes, averages them, and
decodes the result back into the original schema.
}
