% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diffputer-trainer.R
\name{diffputer_trainer}
\alias{diffputer_trainer}
\title{Train a diffusion denoiser on a (possibly imputed) tabular matrix}
\usage{
diffputer_trainer(
  x,
  method = "edm",
  hidden_dim = 1024L,
  epochs = 10000L,
  batch_size = 4096L,
  base_lr = 1e-04,
  patience = 500L,
  scheduler_factor = 0.9,
  scheduler_patience = 50L,
  device = "cpu",
  seed = NULL,
  checkpoint_path = NULL,
  P_mean = -1.2,
  P_std = 1.2,
  sigma_data = 0.5,
  verbose = TRUE
)
}
\arguments{
\item{x}{Numeric matrix or torch tensor of shape \verb{(n, d)}. Missing entries
should already be filled in (e.g. with \code{0} for the first EM iteration, or
with the previous iteration's reconstructions).}

\item{method}{Generative model. \code{"edm"} (default) trains an EDM diffusion
denoiser as in the reference DiffPuter; \code{"flow"} trains a flow-matching
velocity field with linear interpolant noise-to-data paths. Flow models
typically need fewer sampling steps at imputation time.}

\item{hidden_dim}{Hidden width of the MLP denoiser. Defaults to 1024 to
match the reference implementation; set lower (e.g. 256) for small data.}

\item{epochs}{Maximum number of training epochs.}

\item{batch_size}{Mini-batch size.}

\item{base_lr}{Adam learning rate.}

\item{patience}{Early-stopping patience: number of epochs without
improvement on the best loss before stopping.}

\item{scheduler_factor}{Multiplicative factor for \code{ReduceLROnPlateau}.}

\item{scheduler_patience}{Epoch patience for the learning-rate scheduler.}

\item{device}{One of \code{"cpu"}, \code{"cuda"}, or \code{"mps"}. Falls back to \code{"cpu"}
with a warning if the requested accelerator is unavailable.}

\item{seed}{Optional integer seed for R and torch RNGs.}

\item{checkpoint_path}{Optional file path to save the best model
\code{state_dict()} to disk. If \code{NULL}, the best state is kept in memory only.}

\item{P_mean, P_std, sigma_data}{EDM noise distribution parameters. Ignored
when \code{method = "flow"}.}

\item{verbose}{If \code{TRUE}, displays a progress bar with the running loss.}
}
\value{
A list with class \code{trained_RDiffPuter} containing:
\describe{
\item{model}{The fitted \code{diffputer_model} (with best weights loaded).}
\item{denoiser}{Convenience handle for \code{model$denoise_fn_D}.}
\item{optimizer}{The Adam optimizer.}
\item{losses}{Vector of per-epoch mean training losses.}
\item{best_loss}{The lowest loss achieved.}
\item{settings}{List of hyperparameters used.}
}
}
\description{
Runs one M-step of the DiffPuter EM loop: fits an EDM diffusion
model to \code{x} using Adam with a \code{ReduceLROnPlateau} schedule and best-loss
checkpointing. Mirrors the training loop in \code{DiffPuter/main.py}. The
returned object has class \code{trained_RDiffPuter} and is suitable input for
\code{\link[=impute]{impute()}}.
}
\examples{
\dontrun{
  data <- data.frame(a = rnorm(200), b = runif(200))
  transformer <- data_transformer$new()
  transformer$fit(data)
  transformed <- transformer$transform(data)
  trained <- diffputer_trainer(transformed$x, hidden_dim = 64,
                               epochs = 50, batch_size = 64)
}
}
\references{
Zhang, H. et al. "DiffPuter: Empowering Diffusion Models for
Missing Data Imputation." ICLR 2025.
}
