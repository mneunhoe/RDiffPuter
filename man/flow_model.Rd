% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flow-model.R
\name{flow_model}
\alias{flow_model}
\title{Flow-matching model wrapping a velocity MLP}
\usage{
flow_model(denoise_fn)
}
\arguments{
\item{denoise_fn}{The underlying MLP denoiser, e.g. an \code{\link[=mlp_diffusion]{mlp_diffusion()}}
instance. Reused unchanged from the EDM path.}
}
\value{
A \code{torch::nn_module()} generator.
}
\description{
Wraps an \code{\link[=mlp_diffusion]{mlp_diffusion()}} denoiser as a velocity field
\code{v(x, t)} for flow matching. The wrapper has no preconditioning (unlike
the EDM \code{\link[=precond]{precond()}} used by \code{\link[=diffputer_model]{diffputer_model()}}); the underlying MLP
directly outputs a velocity that transports \code{x_0 ~ N(0, I)} at \code{t = 0} to
the data distribution at \code{t = 1}.

On \code{forward(x)} the module computes the per-element flow-matching loss
used by \code{\link[=diffputer_trainer]{diffputer_trainer()}} when \code{method = "flow"}.
}
\references{
Lipman, Y. et al. "Flow Matching for Generative Modeling."
ICLR 2023.
}
