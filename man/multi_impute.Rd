% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multi-impute.R
\name{multi_impute}
\alias{multi_impute}
\alias{multi_impute.trained_RDiffPuter}
\alias{multi_impute.RDiffPuter_em}
\title{Multiple imputation with a trained DiffPuter model}
\usage{
multi_impute(object, data, transformer = NULL, m = 5L, ...)

\method{multi_impute}{trained_RDiffPuter}(
  object,
  data,
  transformer,
  m = 5L,
  num_trials = 1L,
  num_steps = NULL,
  num_resamplings = NULL,
  resample_strategy = "repaint",
  device = NULL,
  seed = NULL,
  ...
)

\method{multi_impute}{RDiffPuter_em}(object, data, transformer = NULL, m = 5L, ...)
}
\arguments{
\item{object}{A \code{trained_RDiffPuter} (from \code{\link[=diffputer_trainer]{diffputer_trainer()}}) or an
\code{RDiffPuter_em} (from \code{\link[=diffputer_em]{diffputer_em()}}).}

\item{data}{The data frame whose \code{NA} entries should be imputed. Required
for both methods (the \code{RDiffPuter_em} object does not retain the original
\code{NA}-containing input).}

\item{transformer}{A fitted \link{data_transformer}. Optional when \code{object} is
an \code{RDiffPuter_em} (the transformer is taken from the object).}

\item{m}{Number of completed data sets to produce.}

\item{...}{Currently unused.}

\item{num_trials}{Within-draw averaging. \code{1} (default) gives Rubin-style
posterior draws; values above 1 reduce within-draw variance but also
shrink between-draw variance, which can hurt pooled standard errors.}

\item{num_steps, num_resamplings, resample_strategy}{Forwarded to \code{\link[=impute]{impute()}}.
Defaults follow the model's \code{method}.}

\item{device}{Device on which to run sampling.}

\item{seed}{Optional integer seed; controls the sequence of \code{m} draws.}
}
\value{
A list of \code{m} data frames, with class \code{mi_imputations} and
attributes \code{m} and \code{method}.
}
\description{
Runs the E-step \code{m} times with fresh random noise and returns
\code{m} independently completed data sets. By default each draw uses
\code{num_trials = 1}, so between-draw variability reflects the model's
posterior uncertainty in the spirit of Rubin's multiple imputation. The
returned object is a list of data frames with class \code{"mi_imputations"};
it can be passed straight to \code{mice::pool()} after fitting an analysis
model on each element.
}
\details{
For pooled inference with the \code{mice} ecosystem, do something like:
\preformatted{
  mi <- multi_impute(em_result, data, m = 10)
  fits <- lapply(mi, function(d) lm(y ~ x1 + x2, data = d))
  summary(mice::pool(fits))
}
}
\examples{
\dontrun{
  set.seed(1)
  data <- data.frame(x = rnorm(200), y = rnorm(200))
  data$x[sample.int(200, 30)] <- NA
  em <- diffputer_em(data, max_iter = 2, epochs = 100, hidden_dim = 64,
                     batch_size = 32, patience = 30)
  mi <- multi_impute(em, data, m = 5)
  fits <- lapply(mi, function(d) lm(y ~ x, data = d))
  summary(mice::pool(fits))
}
}
