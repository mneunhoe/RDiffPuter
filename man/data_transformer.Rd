% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-transformer.R
\name{data_transformer}
\alias{data_transformer}
\title{data_transformer R6 class}
\value{
An R6 generator object.
}
\description{
Preprocesses an arbitrary tabular data set for the diffusion
imputer. Handles continuous (numeric) and discrete (factor / character /
logical) columns, builds the missingness mask from \code{NA}s, and inverts the
transform after imputation. Mirrors the \code{data_transformer} design from the
\code{RGAN} package.
}
\details{
The encoded representation is \verb{(numeric_columns, categorical_columns)} along
the column axis. Numeric columns are standardised with the formula
\code{(x - mean) / sd / 2} (matching the reference DiffPuter implementation), with
mean and sd computed ignoring \code{NA}. Categorical columns are encoded according
to \code{encoding}:
\itemize{
\item \code{"binary"} (default): a column with \code{K} levels becomes
\code{ceil(log2(max(K, 2)))} 0/1 columns whose bit pattern indexes the level.
Mirrors \code{DiffPuter/dataset.py}.
\item \code{"onehot"}: each level becomes its own indicator column.
}

The missingness mask is replicated bit-wise (or level-wise) so that a missing
entry blocks all bits / levels of its original column.

Object methods:
\itemize{
\item \verb{$fit(data, discrete_columns = NULL, encoding = "binary")} learns the
transformation parameters.
\item \verb{$transform(data)} returns a list with components \code{x} (encoded numeric
matrix), \code{mask} (encoded logical matrix; \code{TRUE} = missing), and
\code{encoded_widths}.
\item \verb{$inverse_transform(x_encoded)} returns a data frame in the original
schema, decoding bit-strings or one-hot blocks back to factor levels and
un-standardising numeric columns.
\item \verb{$build_mask(data)} returns the encoded missingness mask alone.
}
}
\section{Public fields}{
\if{html}{\out{<div class="r6-fields">}}
\describe{
\item{\code{column_names}}{Original column names in the input order.}

\item{\code{column_types}}{Per-column type label, \code{"numeric"} or \code{"categorical"}.}

\item{\code{num_idx}}{Integer indices of numeric columns in the original data.}

\item{\code{cat_idx}}{Integer indices of categorical columns in the original
data.}

\item{\code{num_mean}}{Per-numeric-column mean (NA-aware).}

\item{\code{num_sd}}{Per-numeric-column standard deviation (NA-aware).}

\item{\code{cat_levels}}{List of character vectors; one entry per categorical
column giving the level vocabulary in encoding order.}

\item{\code{cat_widths}}{Integer vector; encoded width of each categorical
column under the chosen encoding.}

\item{\code{encoding}}{\code{"binary"} or \code{"onehot"}.}

\item{\code{encoded_dim}}{Total number of columns in the encoded matrix.}

\item{\code{fitted}}{Has \verb{$fit()} been called?}
}
\if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
\itemize{
\item \href{#method-data_transformer-fit}{\code{data_transformer$fit()}}
\item \href{#method-data_transformer-transform}{\code{data_transformer$transform()}}
\item \href{#method-data_transformer-build_mask}{\code{data_transformer$build_mask()}}
\item \href{#method-data_transformer-inverse_transform}{\code{data_transformer$inverse_transform()}}
\item \href{#method-data_transformer-clone}{\code{data_transformer$clone()}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-data_transformer-fit"></a>}}
\if{latex}{\out{\hypertarget{method-data_transformer-fit}{}}}
\subsection{Method \code{fit()}}{
Fit the transformer to a data frame.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{data_transformer$fit(data, discrete_columns = NULL, encoding = "binary")}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{data}}{A data frame (preferred) or matrix.}

\item{\code{discrete_columns}}{Character vector of column names to treat as
discrete. If \code{NULL}, factor / character / logical columns are detected
automatically.}

\item{\code{encoding}}{\code{"binary"} or \code{"onehot"}.}
}
\if{html}{\out{</div>}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-data_transformer-transform"></a>}}
\if{latex}{\out{\hypertarget{method-data_transformer-transform}{}}}
\subsection{Method \code{transform()}}{
Encode a data frame to the numeric matrix form expected by
the diffusion model.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{data_transformer$transform(data)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{data}}{A data frame with the same schema used at \verb{$fit()}.}
}
\if{html}{\out{</div>}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-data_transformer-build_mask"></a>}}
\if{latex}{\out{\hypertarget{method-data_transformer-build_mask}{}}}
\subsection{Method \code{build_mask()}}{
Build the missingness mask only.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{data_transformer$build_mask(data)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{data}}{A data frame with the same schema used at \verb{$fit()}.}
}
\if{html}{\out{</div>}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-data_transformer-inverse_transform"></a>}}
\if{latex}{\out{\hypertarget{method-data_transformer-inverse_transform}{}}}
\subsection{Method \code{inverse_transform()}}{
Decode an encoded numeric matrix back into the original
schema. Numeric columns are un-standardised; categorical columns are
recovered by thresholding bits (binary) or \code{argmax} (one-hot).
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{data_transformer$inverse_transform(x_encoded)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{x_encoded}}{An \verb{n x encoded_dim} numeric matrix or torch tensor.}
}
\if{html}{\out{</div>}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-data_transformer-clone"></a>}}
\if{latex}{\out{\hypertarget{method-data_transformer-clone}{}}}
\subsection{Method \code{clone()}}{
The objects of this class are cloneable with this method.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{data_transformer$clone(deep = FALSE)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{deep}}{Whether to make a deep clone.}
}
\if{html}{\out{</div>}}
}
}
}
